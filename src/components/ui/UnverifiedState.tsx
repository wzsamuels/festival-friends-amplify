import React from "react";

const UnverifiedState: React.FC = () => (
  <div className="m-4 text-xl ">
    Your profile is not verified yet. Once your profile has been verified, you
    will be able to access this feature.
  </div>
);

export default UnverifiedState;
